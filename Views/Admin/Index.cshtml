@model IEnumerable<SkladisteRobe.Models.Korisnik>

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Admin - Users";
}

<div class="container">
    <h2 class="mb-4">Popis Zaposlenika</h2>
    <a asp-action="Create" class="btn btn-primary mb-3">Kreiraj novog zaposlenika</a>
    <table class="table table-striped table-hover" id="usersTable">
        <thead class="table-dark">
            <tr>
                <th>ID</th>
                <th>Korisničko ime</th>
                <th>Ime</th>
                <th>Prezime</th>
                <th>Uloga</th>
                <th>Zadnji Login</th>
                <th>Ukupno Vrijeme Ulogiran</th>
                <th>Akcije</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in Model)
            {
                <tr>
                    <td>@user.Id</td>
                    <td>@user.Username</td>
                    <td>@user.Ime</td>
                    <td>@user.Prezime</td>
                    <td>@user.Role.ToString()</td>
                    <td>@(user.LastLoginTime?.ToString("dd.MM.yyyy HH:mm") ?? "N/A")</td>
                    <td>@user.TotalLoginDuration.ToString(@"hh\:mm\:ss")</td>
                    <td>
                        <form asp-action="ChangeRole" method="post">
                            @Html.AntiForgeryToken()

                            <input type="hidden" name="userId" value="@user.Id" />

                            <select name="role" class="form-select">
                                <option value="Admin" selected="@(user.Role == Uloga.Admin)">Admin</option>
                                <option value="Voditelj" selected="@(user.Role == Uloga.Voditelj)">Voditelj</option>
                                <option value="Zaposlenik" selected="@(user.Role == Uloga.Zaposlenik)">Zaposlenik</option>
                            </select>

                            <button type="submit" class="btn btn-sm btn-primary">Promijeni</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@section Scripts {
    <script>
        $('#usersTable').DataTable({
            language: { url: 'https://cdn.datatables.net/plug-ins/1.13.4/i18n/hr.json' }
        });
    </script>
}